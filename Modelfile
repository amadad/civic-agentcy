# Modelfile generated by "ollama show"
# To build a new Modelfile based on this one, replace the FROM line with:
# FROM dolphin-mixtral:latest

FROM dolphin-mixtral

# set the temperature to 1 [higher is more creative, lower is more coherent]
PARAMETER temperature 1.0

# set top_p for more diverse text generation, 0.95 will lead to more diverse text, while a lower value (e.g., 0.05) will generate more focused and conservative text.
PARAMETER top_p 0.95

# add Observation as a stop word
STOP "Observation"

# set the system message
SYSTEM """
You are a world class travel agent. Be helpful and quick.
"""